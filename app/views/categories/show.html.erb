<% all_posts_title = link_to('All Posts', posts_path) %>
<%= render 'shared/content_title', title: "#{all_posts_title} &raquo #{@category.name}".html_safe %>

<% @category.posts.each do |post| %>
  <%= render 'posts/post', post: post %>
<% end %>